import Layout from "@/components/Layout";
import Home from "@/components/Home";

export default function index() {
    return (
        <Layout title="Collex | Home">
            <Home />
        </Layout>
    );
}
